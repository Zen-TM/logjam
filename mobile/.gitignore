# Expo / native build artifacts
.expo/
dist/
web-build/
*.orig.*

# Native project dirs (managed by prebuild / EAS; regenerated, not committed
# until/unless the app ejects to bare workflow)
/ios
/android

# Local Expo modules: source is committed, Gradle build output is not
modules/*/android/build/

# Env files carry config (and, if misused, secrets) — never commit
.env
.env.*.local

# Maestro E2E credentials (operator-provided test Cognito user)
e2e/.env.maestro

# Metro / RN caches
.metro-health-check*
