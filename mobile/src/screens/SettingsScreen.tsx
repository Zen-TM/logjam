// Settings — theme scheme, notification preferences, auto-download. Not built
// yet; the route is wired so the More hub has a complete menu. Placeholder
// until the settings surface lands.
import { EmptyState } from "../ui";

export function SettingsScreen() {
  return (
    <EmptyState
      title="Settings"
      hint="Theme, notifications and download preferences are coming soon."
    />
  );
}
